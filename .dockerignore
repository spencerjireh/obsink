target/
**/node_modules/
desktop/dist/
desktop/src-tauri/gen/
ios/
.git/
.env
.env.*
!.env.example
server/data/
# Docker builds use the image's own toolchain (rust:1.97-bookworm); without this rustup
# would install the pinned toolchain + components inside every build.
rust-toolchain.toml
