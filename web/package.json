{
  "name": "obsink-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "ObSink in the browser: the shared screens over a worker that syncs a local folder through the File System Access API",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@obsink/ui": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/wicg-file-system-access": "^2023.10.6",
    "fake-indexeddb": "^6.2.2",
    "playwright": "^1.63.0",
    "vitest": "^3.2.4"
  }
}
